# Sayeed Courses Video API

Backend for Sayeed Courses Hub video delivery.

Current engine: Telegram private channel + GramJS/MTProto + HTTP Range streaming.

Phase 1 adds Firebase Admin authentication support, Firestore enrollment checks, configurable CORS, and scan caching while preserving the tested streaming engine.

See `PHASE-1.md` for deployment instructions.
